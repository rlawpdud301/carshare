<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.mapper.RouteMapper">
	<insert id="insertRoute">
		INSERT INTO carshare.route
		(Route_no, id, process, start_address, start_latitude, start_hardness,
		end_address, end_latitude, end_hardness)
		VALUES(#{RouteNo},#{id.id}, #{process}, #{startAddress}, #{startLatitude}, #{startHardness}, #{endAddress}, #{endLatitude}, #{endHardness})
	</insert>
</mapper> 